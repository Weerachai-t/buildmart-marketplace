"use client";import type{Product}from "@/lib/types";
export function addCart(id:string,qty=1){const cart=JSON.parse(localStorage.getItem("buildmart-cart")||"[]") as {id:string;qty:number}[];const found=cart.find(x=>x.id===id);if(found)found.qty+=qty;else cart.push({id,qty});localStorage.setItem("buildmart-cart",JSON.stringify(cart));window.dispatchEvent(new Event("cart-updated"));}
export default function ProductCard({p}:{p:Product}){return <article className="product-card"><a href={`/products/${p.slug}`}><div className="product-image"><span className="badge">{p.badges[0]}</span><strong>{p.image}</strong></div><div className="product-info"><small>{p.brand} · {p.category}</small><h3>{p.name}</h3><div className="rating">★★★★★ <span style={{color:"#8796a5"}}>{p.rating}</span></div><div className="price">฿{p.price.toLocaleString()} <small>/ {p.unit}</small></div></div></a><div className="product-info" style={{paddingTop:0}}><button className="btn" onClick={()=>addCart(p.id)}>เพิ่มลงตะกร้า</button></div></article>}
