import{NextResponse}from"next/server";import{z}from"zod";const schema=z.object({companyName:z.string().min(2),taxId:z.string().min(10),contactName:z.string().min(2),phone:z.string().min(9),email:z.string().email()});export async function POST(req:Request){const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"ข้อมูลไม่ถูกต้อง",details:parsed.error.flatten()},{status:400});return NextResponse.json({success:true,status:"PENDING",applicationNumber:`SUP-${Date.now()}`},{status:201})}
