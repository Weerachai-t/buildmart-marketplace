import{NextResponse}from"next/server";import{z}from"zod";const schema=z.object({items:z.array(z.object({productId:z.string(),quantity:z.number().positive()})).min(1),paymentMethod:z.enum(["BANK_TRANSFER","QR_PAYMENT","CORPORATE_CREDIT"])});export async function POST(req:Request){const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"ข้อมูลคำสั่งซื้อไม่ถูกต้อง"},{status:400});return NextResponse.json({orderNumber:`BM-${Date.now()}`,status:"PENDING",paymentStatus:"PENDING"},{status:201})}
