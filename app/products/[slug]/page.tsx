import Header from"@/components/header";import Footer from"@/components/footer";import{products}from"@/lib/data";import ProductClient from"./product-client";import{notFound}from"next/navigation";
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <><Header/><main className="container section"><p style={{fontSize:10}}><a href="/">หน้าหลัก</a> › <a href="/products">สินค้า</a> › {p.name}</p><ProductClient p={p}/></main><Footer/></>}
