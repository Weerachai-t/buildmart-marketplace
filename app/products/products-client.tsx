"use client";import{useMemo,useState}from"react";import ProductCard from"@/components/product-card";import type{Product}from"@/lib/types";
export default function ProductsClient({products,categories}:{products:Product[];categories:string[]}){const[q,setQ]=useState(""),[cat,setCat]=useState("ทั้งหมด");const shown=useMemo(()=>products.filter(p=>(cat==="ทั้งหมด"||p.category===cat)&&(`${p.name} ${p.brand}`.toLowerCase().includes(q.toLowerCase()))),[q,cat,products]);return <><div className="panel" style={{padding:18,display:"flex",gap:12,marginBottom:20}}><input style={{flex:1,padding:12,border:"1px solid #dfe5eb"}} value={q} onChange={e=>setQ(e.target.value)} placeholder="ค้นหาชื่อสินค้า แบรนด์ หรือรายละเอียด"/><select style={{padding:12,border:"1px solid #dfe5eb"}} value={cat} onChange={e=>setCat(e.target.value)}><option>ทั้งหมด</option>{categories.map(c=><option key={c}>{c}</option>)}</select></div><div className="product-grid">{shown.map(p=><ProductCard p={p} key={p.id}/>)}</div></>}
